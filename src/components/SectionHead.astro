---
interface Props {
  eyebrow: string;
  title: string;
  lede?: string;
}

const { eyebrow, title, lede } = Astro.props;
---

<div class="sec-h rv">
  <span class="eyebrow">{eyebrow}</span>
  <h2>{title}</h2>
  {lede && <p class="lede">{lede}</p>}
</div>
